"""Federated simulation (P1.1). FedAvg client/server loop, one client = one user; shared item
embeddings are the federated object. To add: simulator.py (round-resumable via src.runtime).
Until then, experiments/run.py uses the centralized-equivalent MF backbone (validated in P0)."""
