# Confirmatory (an HPC cluster): mf on gowalla (dense diverse-5). 5000-user subsample (full=6.4M interactions).
sweep_id: mf_gowalla
backbone: mf
dataset: gowalla
n_users: 5000
data_seed: 0
dim: 32
epochs: 30

seeds: [0, 1, 2, 3, 4]
per_stratum: 60
n_controls: 50
methods: [retrain, naive_delete, gradient_ascent, finetune]
