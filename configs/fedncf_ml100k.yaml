# Confirmatory (an HPC cluster): federated GMF (FedNCF) on full ML-100K (adds FRU + ours).
sweep_id: fedncf_ml100k
backbone: fedncf
dataset: ml100k
n_users: 0             # full ML-100K
data_seed: 0
dim: 32
fed_rounds: 30
fed_local_epochs: 3
fed_lr: 0.2
seeds: [0, 1, 2, 3, 4]
per_stratum: 60
n_controls: 50
methods: [retrain, naive_delete, gradient_ascent, fru, ours]
